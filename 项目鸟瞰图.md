# USStock 项目鸟瞰图

## 项目目标

USStock 是一个面向美股投研的自动发现系统。当前目标是把 SEC、GDELT、Finnhub 等数据源串成一条每日工作流：

- 自动发现市场热点
- 将热点映射到相关美股标的
- 用结构化财务数据做硬筛选
- 后续用 AI 辅助阅读财报文本和新闻材料
- 输出每日候选股评分、观察清单和交易计划

## 总体架构

```mermaid
flowchart TB
    User["姜楠 / 本地用户"] --> Admin["本地控制面板<br/>src/usstock/admin/app.py"]
    User --> CLI["命令行入口<br/>usstock CLI"]

    Admin --> Tasks["同步任务页 /tasks"]
    Tasks --> DiscoveryAction["自动热点发现<br/>运行一次 / 启动定时 / 停止定时"]
    CLI --> DiscoveryCLI["usstock discover daily / loop"]

    DiscoveryAction --> Discovery["自动发现工作流<br/>src/usstock/discovery/daily.py"]
    DiscoveryCLI --> Discovery

    Discovery --> GDELT["GDELT DOC API<br/>全球新闻和主题热度"]
    Discovery --> Finnhub["Finnhub News API<br/>市场新闻和个股新闻"]
    Discovery --> SEC["SEC EDGAR API<br/>公告和财务 facts"]

    GDELT --> RawDB["PostgreSQL 原始数据层"]
    Finnhub --> RawDB
    SEC --> RawDB

    RawDB --> DiscoveryTables["自动发现中间层"]
    DiscoveryTables --> Scores["每日候选股评分"]
    Scores --> Watchlist["每日观察清单"]
    Watchlist --> Admin
```

## 当前自动发现工作流

```mermaid
flowchart LR
    Start["触发入口"] --> TriggerType{"触发方式"}
    TriggerType --> Once["控制面板点击运行一次"]
    TriggerType --> Schedule["控制面板启动定时任务"]
    TriggerType --> Command["usstock discover daily / loop"]

    Once --> Seed["写入 / 刷新默认主题库<br/>market_topics"]
    Schedule --> Seed
    Command --> Seed

    Seed --> FinnhubSync["同步 Finnhub market news<br/>默认 general + merger"]
    Seed --> GdeltSync["按主题库同步 GDELT<br/>替代人工输入关键词"]
    Seed --> SecSync["按股票池扫描 SEC filings"]

    FinnhubSync --> Extract["抽取 ticker、关键词、主题命中"]
    GdeltSync --> Extract
    SecSync --> Extract

    Extract --> Mentions["写入主题提及<br/>topic_mentions"]
    Mentions --> Score["简单评分模型"]
    Score --> CandidateScores["写入每日候选评分<br/>daily_candidate_scores"]
    CandidateScores --> Watchlists["写入每日观察清单<br/>daily_watchlists"]
```

## 数据源分工

```mermaid
flowchart TB
    SEC["SEC EDGAR"] --> SECUse["硬事实来源<br/>8-K / 10-Q / 10-K / S-1 / company facts"]
    Finnhub["Finnhub News"] --> FinnhubUse["财经新闻入口<br/>market news / company news / related_tickers"]
    GDELT["GDELT DOC"] --> GDELTUse["宏观和行业热度<br/>主题新闻数量 / 时间线 / 跨区域扩散"]

    SECUse --> Screening["候选股硬筛选"]
    FinnhubUse --> Discovery["热点和 ticker 自动发现"]
    GDELTUse --> Trend["主题热度验证"]

    Discovery --> Score["综合评分"]
    Trend --> Score
    Screening --> Score
```

## 数据库分层

```mermaid
flowchart TB
    Universe["stock_universe<br/>股票池"] --> DiscoveryLayer["自动发现层"]

    subgraph Raw["原始数据层"]
        SECRegistry["sec_company_registry"]
        SECFilings["sec_filings"]
        SECFacts["sec_financial_facts"]
        GdeltArticles["gdelt_articles"]
        GdeltTimeline["gdelt_timeline_points"]
        FinnhubArticles["finnhub_articles"]
    end

    Raw --> DiscoveryLayer

    subgraph DiscoveryLayer["自动发现层"]
        Topics["market_topics<br/>主题库"]
        Mentions["topic_mentions<br/>主题提及"]
        Scores["daily_candidate_scores<br/>每日候选评分"]
        Watchlists["daily_watchlists<br/>每日观察清单"]
    end

    Topics --> Mentions
    Mentions --> Scores
    Scores --> Watchlists
```

## 控制面板任务区

```mermaid
flowchart LR
    Tasks["/tasks 同步任务页"] --> DiscoveryCard["自动热点发现卡片"]
    DiscoveryCard --> RunOnce["运行一次"]
    DiscoveryCard --> StartLoop["启动定时"]
    DiscoveryCard --> StopLoop["停止定时"]

    RunOnce --> Daily["run_daily_discovery"]
    StartLoop --> Scheduler["DiscoveryScheduler<br/>面板进程内守护线程"]
    Scheduler --> Daily
    StopLoop --> Scheduler

    Daily --> Result["候选股评分和观察清单"]
```

## 当前 CLI 命令

```bash
usstock admin # 启动本地控制面板，默认访问 http://127.0.0.1:7878。
usstock admin --port 7879 # 使用 7879 端口启动控制面板，适合默认端口被占用时使用。
usstock migrate # 执行数据库迁移，创建或更新项目所需表结构。
usstock discover seed-topics # 写入或刷新自动发现使用的默认市场主题库。
usstock discover daily --top-n 25 # 同步数据并运行一次每日热点发现，输出前 25 个候选标的。
usstock discover daily --skip-sync --top-n 25 # 跳过外部数据同步，仅基于库内已有数据重新生成前 25 个候选标的。
usstock discover loop --interval-minutes 60 # 每 60 分钟循环运行一次自动热点发现任务。
```

## 关键文件

- `src/usstock/cli.py`：统一 CLI 入口。
- `src/usstock/admin/app.py`：本地控制面板，包含自动发现按钮和定时任务控制。
- `src/usstock/discovery/daily.py`：自动热点发现、主题映射、评分和观察清单生成。
- `src/usstock/data/gdelt.py`：GDELT DOC API 同步。
- `src/usstock/data/finnhub.py`：Finnhub News API 同步。
- `src/usstock/data/sec.py`：SEC EDGAR 同步。
- `migrations/006_create_discovery_tables.sql`：自动发现相关表结构。

## 当前能力边界

当前版本已经能把“人工输入关键词查新闻”升级为“系统按主题库自动发现热点”。不过它仍是 MVP：

- 定时任务运行在本地控制面板进程内，面板重启后需要重新启动。
- 评分模型是规则模型，适合初筛，不等于交易建议。
- 暂未接入行情异动、技术指标、期权异动和 AI 财报全文阅读。
- 后续可以把控制面板内定时任务升级为 cron、systemd、Celery 或独立 worker。
